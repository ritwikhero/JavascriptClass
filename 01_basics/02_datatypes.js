"use strict" // treat all code as new version of js , its evolved so no need to use but a good use case of it 

//alert(3+3);  we are using node js not browser 

//code readability should be given high priority

let name = "Ritwik"; //string
let age = 26; //integer
let isLoggeIn = true; //boolean

//null => is stand alone value
//undefined => variable is created but not value is given

console.log(typeof name);
console.log(typeof age);
